Steps to Be followed ::

Step 1 : Create Fils/Folders
Step 2 : Move to the Files via command through BASH/Terminal
Step 3 : Add Contents
Step 4 : Commit Changes by following command as "git commit -m "<message>" --patch
Step 5 : Make Changes through the following command as "git push"

// If You're Unable to execute chnages via `git push` // 
/*
    Then Add Changes - 
    View What to Change :: `git status`
        If Files are asked to be Added then, 
        Add the Files using :: `git add <path/file_name>`
    Then Again, See Changes,
        `git status` 
    T
*/

// Then Exexute `git commit -m <message>` //
/*
    Repeat Step 5 : So as to make Permanent Changes to the Remote Repository.
*/